using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fuzzy_rules : MonoBehaviour
{
    public Fuzzy_logic l = new Fuzzy_logic();

    private void Rules()
    {
        // Расстояния
        //if ((l.Distance_left == "БЛИЗКО") && ()) { l.Speed = "БЫСТРО" }

    }
}
